spring.application.name=ProjectBlog

# Railway dynamic port assignment
server.port=${PORT:8080}
server.address=0.0.0.0

# MongoDB Atlas Connection using Environment Variable
spring.data.mongodb.uri=${MONGODB_URI}